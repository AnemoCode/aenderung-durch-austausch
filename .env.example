DJANGO_SECRET_KEY=change-me-generate-a-long-random-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

POSTGRES_DB=overlytics
POSTGRES_USER=overlytics
POSTGRES_PASSWORD=overlytics
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Fernet key for encrypting Overleaf tokens at rest.
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# WARNING: changing this key will make existing stored tokens unreadable.
FIELD_ENCRYPTION_KEY=

REDIS_URL=redis://redis:6379/0
